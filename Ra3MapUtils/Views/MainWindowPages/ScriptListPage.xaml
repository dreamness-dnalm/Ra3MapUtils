<Page x:Class="Ra3MapUtils.Views.MainWindowPages.ScriptListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Ra3MapUtils.Views.MainWindowPages"
      xmlns:vm="clr-namespace:Ra3MapUtils.ViewModels.MainWindowPages"
      d:DataContext="{d:DesignInstance Type=vm:ScriptListPageViewModel, IsDesignTimeCreatable=True}"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
      ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
      xmlns:converters="clr-namespace:Ra3MapUtils.Utils.XamlConverters"
      xmlns:validationRules="clr-namespace:Ra3MapUtils.Utils.XamlValidationRules"
      xmlns:controls="clr-namespace:Ra3MapUtils.Services.Controls"
      mc:Ignorable="d"
      Title="ScriptListPage" Height="450" Width="800"
      >
    <Grid Margin="15, 15, 15, 15" >
        <!-- <Grid VerticalAlignment="Top" HorizontalAlignment="Stretch"> -->
        <!--      -->
        <!-- </Grid> -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <!-- <RowDefinition/> -->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>
        
        <Menu Grid.Row="0" Grid.Column="0">
            <ui:MenuItem Icon="{ui:SymbolIcon Add20}" ToolTip="新建" Command="{Binding AddScriptCommand}"/>
            <ui:MenuItem Icon="{ui:SymbolIcon ArrowSync20}" ToolTip="刷新列表" Command="{Binding RefreshScriptListCommand}"/>
            <ui:MenuItem Icon="{ui:SymbolIcon FolderOpen20}" ToolTip="打开文件夹" Command="{Binding OpenInExplorerCommand}"/>
            <ui:MenuItem Icon="{ui:SymbolIcon Rename20}" ToolTip="重命名" Command="{Binding RenameScriptCommand}"/>
            <ui:MenuItem Icon="{ui:SymbolIcon Copy20}" ToolTip="另存为" Command="{Binding CloneScriptCommand}"/>
            <ui:MenuItem Icon="{ui:SymbolIcon Delete20}" ToolTip="删除" Command="{Binding DeleteScriptCommand}"/>

        </Menu>
    </Grid>
</Page>
