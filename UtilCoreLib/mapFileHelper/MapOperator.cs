using System;
using System.IO;
// using MapCoreLib.Core;
// using MapCoreLib.Core.Asset;

namespace UtilLib.mapFileHelper
{
    public class MapOperator
    {
        public static void Main()
        {
            var mapName = "NewMap33";
            Console.WriteLine(mapName);
            // var (mapDir, _) = MapFileHelper.TranslateMapPath(mapName);
            // // MapDataContext
            // var ra3Map = new Ra3Map(Path.Combine(mapDir, mapName + ".map"));
            // ra3Map.parse();
            // var context = ra3Map.getContext();
            // // var scriptList = context.getAsset<ScriptList>("");
            // var mapScriptListener = new MapScriptListener();
            // ra3Map.visit(mapScriptListener);
            //
            //
            // ra3Map.doSaveMap(ra3Map.mapPath);
            
            // Assembly assembly = Assembly.LoadFrom(@"H:\workspace\dreamness_ra3_tools\UtilCoreLib\bin\Debug\net8.0\UtilCoreLib.dll");
            // //    var type = assembly.GetType("UtilCoreLib.mapScriptHelper.MapScriptHelper");
            // var types = assembly.GetTypes().Where(t => t.IsClass);

        }
    }
}