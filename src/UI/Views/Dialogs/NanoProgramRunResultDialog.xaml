<Window x:Class="UI.Views.Dialogs.NanoProgramRunResultDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Nano_RunResultTitle}"
        Width="720"
        Height="520"
        WindowStartupLocation="CenterOwner"
        FontFamily="Segoe UI Variable Text, Segoe UI">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <DockPanel Grid.Row="0">
      <TextBlock Text="{Binding ProgramName}" FontSize="20" FontWeight="SemiBold" />
      <TextBlock DockPanel.Dock="Right"
                 Text="{Binding TimeText}"
                 Opacity="0.65"
                 VerticalAlignment="Center" />
      <Border DockPanel.Dock="Right"
              Margin="12,0"
              Padding="8,3"
              CornerRadius="6"
              VerticalAlignment="Center">
        <Border.Style>
          <Style TargetType="Border">
            <Setter Property="Background" Value="#C62828" />
            <Style.Triggers>
              <DataTrigger Binding="{Binding Success}" Value="True">
                <Setter Property="Background" Value="#2E7D32" />
              </DataTrigger>
            </Style.Triggers>
          </Style>
        </Border.Style>
        <TextBlock Text="{Binding StatusText}" Foreground="White" FontWeight="SemiBold" />
      </Border>
    </DockPanel>

    <TextBlock Grid.Row="1" Margin="0,14,0,4" FontWeight="SemiBold" Text="{DynamicResource Nano_ReturnValue}" />
    <DockPanel Grid.Row="2">
      <Button DockPanel.Dock="Right"
              Content="{DynamicResource Nano_Copy}"
              Margin="8,0,0,0"
              Padding="10,4"
              Command="{Binding CopyCommand}"
              CommandParameter="{Binding ReturnValueText}" />
      <TextBox Text="{Binding ReturnValueText, Mode=OneWay}"
               IsReadOnly="True"
               AcceptsReturn="True"
               TextWrapping="Wrap"
               Height="56"
               VerticalScrollBarVisibility="Auto" />
    </DockPanel>

    <Grid Grid.Row="3" Margin="0,12,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>

      <TextBlock Grid.Column="0" Text="{DynamicResource Nano_Output}" FontWeight="SemiBold" />
      <TextBlock Grid.Column="1" Text="{DynamicResource Nano_Errors}" FontWeight="SemiBold" Margin="8,0,0,0" />

      <TextBox Grid.Row="1"
               Grid.Column="0"
               Margin="0,6,4,0"
               Text="{Binding OutputText, Mode=OneWay}"
               IsReadOnly="True"
               AcceptsReturn="True"
               TextWrapping="Wrap"
               VerticalScrollBarVisibility="Auto" />
      <TextBox Grid.Row="1"
               Grid.Column="1"
               Margin="4,6,0,0"
               Text="{Binding ErrorText, Mode=OneWay}"
               IsReadOnly="True"
               AcceptsReturn="True"
               TextWrapping="Wrap"
               VerticalScrollBarVisibility="Auto" />

      <Button Grid.Row="2"
              Grid.Column="0"
              HorizontalAlignment="Right"
              Margin="0,8,4,0"
              Content="{DynamicResource Nano_CopyOutput}"
              Padding="10,4"
              Command="{Binding CopyCommand}"
              CommandParameter="{Binding OutputText}" />
      <Button Grid.Row="2"
              Grid.Column="1"
              HorizontalAlignment="Right"
              Margin="4,8,0,0"
              Content="{DynamicResource Nano_CopyErrors}"
              Padding="10,4"
              Command="{Binding CopyCommand}"
              CommandParameter="{Binding ErrorText}" />
    </Grid>
  </Grid>
</Window>
