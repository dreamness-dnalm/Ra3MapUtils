<Window x:Class="UI.Views.Tools.FastHashCalculatorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:controls="clr-namespace:UI.Controls"
        Title="{DynamicResource FastHash_Title}"
        Height="620"
        Width="980"
        MinHeight="480"
        MinWidth="760"
        WindowStartupLocation="CenterOwner"
        Topmost="{Binding IsTopmost}"
        FontFamily="Segoe UI Variable Text, Segoe UI">
  <shell:WindowChrome.WindowChrome>
    <shell:WindowChrome CaptionHeight="40"
                        ResizeBorderThickness="6"
                        GlassFrameThickness="0"
                        UseAeroCaptionButtons="False" />
  </shell:WindowChrome.WindowChrome>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="40" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <controls:ToolWindowTitleBar Grid.Row="0" />

    <Grid Grid.Row="1" Margin="16,8,16,16">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <DockPanel Grid.Row="0" Margin="0,0,0,12">
        <Button DockPanel.Dock="Right"
                Content="{DynamicResource FastHash_Copy}"
                Padding="12,6"
                Command="{Binding CopyOutputCommand}" />
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
          <CheckBox Content="{DynamicResource FastHash_CaseInsensitive}"
                    IsChecked="{Binding IsCaseInsensitive}"
                    Margin="0,0,16,0" />
          <CheckBox Content="{DynamicResource FastHash_HexOutput}"
                    IsChecked="{Binding IsHexOutput}"
                    Margin="0,0,16,0" />
          <TextBlock Text="{DynamicResource FastHash_Hint}"
                     Opacity="0.65"
                     VerticalAlignment="Center" />
        </StackPanel>
      </DockPanel>

      <Grid Grid.Row="1">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="8" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <DockPanel Grid.Column="0">
          <TextBlock DockPanel.Dock="Top" Text="{DynamicResource FastHash_Input}" FontWeight="SemiBold" Margin="0,0,0,6" />
          <TextBox Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                   AcceptsReturn="True"
                   AcceptsTab="True"
                   TextWrapping="NoWrap"
                   FontFamily="Consolas"
                   VerticalScrollBarVisibility="Auto"
                   HorizontalScrollBarVisibility="Auto" />
        </DockPanel>

        <GridSplitter Grid.Column="1" Width="8" HorizontalAlignment="Stretch" />

        <DockPanel Grid.Column="2">
          <TextBlock DockPanel.Dock="Top" Text="{DynamicResource FastHash_Output}" FontWeight="SemiBold" Margin="0,0,0,6" />
          <TextBox Text="{Binding OutputText, Mode=OneWay}"
                   IsReadOnly="True"
                   AcceptsReturn="True"
                   TextWrapping="NoWrap"
                   FontFamily="Consolas"
                   VerticalScrollBarVisibility="Auto"
                   HorizontalScrollBarVisibility="Auto" />
        </DockPanel>
      </Grid>
    </Grid>
  </Grid>
</Window>
