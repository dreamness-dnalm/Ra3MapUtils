<Window x:Class="UI.Views.Tools.LuaImportManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:controls="clr-namespace:UI.Controls"
        xmlns:vm="clr-namespace:UI.ViewModels"
        Title="{Binding WindowTitle}"
        Height="560"
        Width="820"
        MinHeight="420"
        MinWidth="640"
        WindowStartupLocation="CenterOwner"
        Topmost="{Binding IsTopmost}"
        FontFamily="Segoe UI Variable Text, Segoe UI"
        Closed="LuaImportManagerWindow_OnClosed">
  <shell:WindowChrome.WindowChrome>
    <shell:WindowChrome CaptionHeight="40"
                        ResizeBorderThickness="6"
                        GlassFrameThickness="0"
                        UseAeroCaptionButtons="False" />
  </shell:WindowChrome.WindowChrome>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="40" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <controls:ToolWindowTitleBar Grid.Row="0" />

    <Grid Grid.Row="1" Margin="16,8,16,8">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <DockPanel Grid.Row="0" Margin="0,0,0,10">
        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
          <Button Content="{DynamicResource LuaImport_Add}"
                  Padding="12,6"
                  Margin="0,0,8,0"
                  Command="{Binding AddLibraryCommand}" />
          <Button Content="{DynamicResource LuaImport_Import}"
                  Padding="12,6"
                  Command="{Binding ImportCommand}" />
        </StackPanel>
        <TextBlock Text="{Binding MapFilePath}"
                   Opacity="0.65"
                   TextTrimming="CharacterEllipsis"
                   VerticalAlignment="Center" />
      </DockPanel>

      <Grid Grid.Row="1">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="12" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <ListBox Grid.Column="0"
                 ItemsSource="{Binding Libraries}"
                 SelectedItem="{Binding SelectedLibrary}"
                 BorderBrush="#22000000"
                 BorderThickness="1">
          <ListBox.ItemTemplate>
            <DataTemplate DataType="{x:Type vm:LuaLibConfigItemViewModel}">
              <Grid Margin="4,6">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="Auto" />
                  <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <CheckBox Grid.Column="0"
                          IsChecked="{Binding IsEnabled, Mode=TwoWay}"
                          VerticalAlignment="Center"
                          Margin="0,0,8,0" />
                <StackPanel Grid.Column="1">
                  <TextBlock Text="{Binding ShowingName}" FontWeight="SemiBold" />
                  <TextBlock Text="{Binding LibPath}"
                             Opacity="0.65"
                             FontSize="11"
                             TextTrimming="CharacterEllipsis" />
                </StackPanel>
              </Grid>
            </DataTemplate>
          </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Column="2" Width="110">
          <Button Content="{DynamicResource LuaImport_Rename}"
                  Margin="0,0,0,8"
                  Padding="8,6"
                  Command="{Binding RenameLibraryCommand}" />
          <Button Content="{DynamicResource LuaImport_ChangePath}"
                  Margin="0,0,0,8"
                  Padding="8,6"
                  Command="{Binding ChangePathCommand}" />
          <Button Content="{DynamicResource LuaImport_MoveUp}"
                  Margin="0,0,0,8"
                  Padding="8,6"
                  Command="{Binding MoveUpCommand}" />
          <Button Content="{DynamicResource LuaImport_MoveDown}"
                  Margin="0,0,0,8"
                  Padding="8,6"
                  Command="{Binding MoveDownCommand}" />
          <Button Content="{DynamicResource LuaImport_Remove}"
                  Margin="0,0,0,8"
                  Padding="8,6"
                  Command="{Binding RemoveLibraryCommand}" />
          <Button Content="{DynamicResource LuaImport_Docs}"
                  Padding="8,6"
                  Command="{Binding OpenDocsCommand}" />
        </StackPanel>
      </Grid>
    </Grid>

    <TextBlock Grid.Row="2"
               Margin="16,0,16,12"
               Opacity="0.65"
               Text="{Binding StatusMessage}" />
  </Grid>
</Window>
